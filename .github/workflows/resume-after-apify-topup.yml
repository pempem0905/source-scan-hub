name: Resume After Apify Topup
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/resume-after-apify-topup.yml
permissions:
  contents: read
jobs:
  resume:
    runs-on: ubuntu-latest
    steps:
      - name: Resume native scanner
        run: |
          node <<'NODE'
          (async()=>{
            const res=await fetch('https://source-scan-hub.lovable.app/api/temp-resume-apify120',{
              method:'POST',
              headers:{'content-type':'application/json','x-one-shot':'resume120-20260816-1449-x7p4'},
              body:'{}'
            });
            const text=await res.text();
            console.log(text);
            if(!res.ok) process.exit(1);
          })().catch(e=>{console.error(e);process.exit(1)});
          NODE
