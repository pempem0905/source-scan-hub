name: Resume Apify Native Once
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/resume-apify-native-once.yml
permissions:
  contents: read
jobs:
  resume:
    runs-on: ubuntu-latest
    steps:
      - name: Resume native Apify scanner
        run: |
          node <<'NODE'
          (async()=>{
            const res=await fetch('https://source-scan-hub.lovable.app/api/temp-sync-apify-display',{
              method:'POST',
              headers:{'content-type':'application/json','x-one-shot':'9sTcuUbRnSlKRewYEhJ9TNdxoGqpaN24'},
              body:JSON.stringify({action:'backfillRun'})
            });
            const text=await res.text();
            console.log(text);
            if(!res.ok) process.exit(1);
          })().catch(e=>{console.error(e);process.exit(1)});
          NODE
