name: Resume Apify 80 Once
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/resume-apify80-once.yml
permissions:
  contents: read
jobs:
  resume:
    runs-on: ubuntu-latest
    steps:
      - name: Wait for production publish
        run: sleep 45
      - name: Raise project cap to 80 and resume
        run: |
          node <<'NODE'
          (async()=>{
            const res=await fetch('https://source-scan-hub.lovable.app/api/temp-resume-apify80',{
              method:'POST',
              headers:{'content-type':'application/json','x-one-shot':'resume80-20260816'},
              body:'{}'
            });
            const text=await res.text();
            console.log(text);
            if(!res.ok) process.exit(1);
          })().catch(e=>{console.error(e);process.exit(1)});
          NODE
